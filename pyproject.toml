[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["UR5_coppelia"]

[project]
name = "UR5_coppelia"
version = "0.0.1"
dependencies = [
    "gymnasium",
    "coppeliasim_zmqremoteapi_client",
    "numpy",
]
